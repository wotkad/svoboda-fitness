extends ../layouts/master

block title
  title Свобода фитнес | Абонементы

block basicSeo
  meta(content="Абонементы" name="description")
  meta(content="Абонементы" name="keywords")

block manifestBrowserconfigFiles
  link(href="../manifest.json" rel="manifest" crossorigin)
  meta(content="../browserconfig.xml" name="msapplication-config")

block content
  main
    .barba(data-barba="wrapper")
      .barba-container(data-barba="container" data-barba-namespace="plans")
        .wrapper
          .headline
            +breadcrumbs([ { title: 'Абонементы', href: '' } ])
            +subtitle('01.', 'Абонементы')
        .pusher
          section.plans
            include ../components/plans