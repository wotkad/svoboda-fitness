extends ../layouts/master

block title
  title Свобода фитнес | Купить абонемент

block basicSeo
  meta(content="Купить абонемент" name="description")
  meta(content="Купить абонемент" name="keywords")

block manifestBrowserconfigFiles
  link(href="../manifest.json" rel="manifest" crossorigin)
  meta(content="../browserconfig.xml" name="msapplication-config")

block content
  main
    .barba(data-barba="wrapper")
      .barba-container(data-barba="container" data-barba-namespace="order")
        .wrapper
          .headline
            +breadcrumbs([ { title: 'Абонементы', href: '/plans/' }, { title: 'Купить абонемент', href: ''} ])
            +subtitle('01.', 'Купить абонемент')
        .pusher
          include ../components/order