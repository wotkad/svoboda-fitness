extends ../layouts/master

block title
  title Свобода фитнес | Тренеры

block basicSeo
  meta(content="Тренеры" name="description")
  meta(content="Тренеры" name="keywords")

block manifestBrowserconfigFiles
  link(href="../manifest.json" rel="manifest" crossorigin)
  meta(content="../browserconfig.xml" name="msapplication-config")

block content
  main
    .barba(data-barba="wrapper")
      .barba-container(data-barba="container" data-barba-namespace="coaches")
        .wrapper
          .headline
            +breadcrumbs([ { title: 'Тренеры', href: '' } ])
            +subtitle('01.', 'Тренеры')
          .pusher
            include ../components/coaches